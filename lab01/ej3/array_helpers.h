unsigned int array_from_file (int array[], unsigned int max_size, const char* filepath);

void array_dump (int a[], unsigned int length);

/* 
Basicamente el array_helpers.h es la especificacion del TAD
Aca escribimos los prototipos de las funciones que vamos a utilizar, es decir que devuelve, el no-
bre de la funcion, los párametros que toma y por ultimo algun comentario (opcional).
 */